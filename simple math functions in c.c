#include <stdio.h>
#include <math.h>

int main(){

float x=9;
//x=sqrt(x);
//x=pow(x,3);
//x=round(x);
//x=ceil(x);
//x=floor(x);
//(ceil means rounding up,floor means rounding down);
//x=log(x);


printf("%f",x);



    return 0;
}